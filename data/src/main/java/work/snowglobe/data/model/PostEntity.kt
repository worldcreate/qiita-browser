package work.snowglobe.data.model

/**
 * Representation for a [PostEntity] fetched from an external layer data source
 */
data class PostEntity(val name: String, val title: String, val avatar: String)